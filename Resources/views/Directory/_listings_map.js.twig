<script type="text/javascript">
    var listingMapData = new Object;

    {% for listing in mapListings if listing.location.lat is defined and listing.location.lat != "" and listing.location.lng is defined and listing.location.lng != "" %}
        {% include 'CCETCDirectoryBundle:Directory:_listings_marker_object.js.twig' %}
        listingMapData["{{listing.id}}"] = listing;
    {% endfor %}            

    var map;  
</script>