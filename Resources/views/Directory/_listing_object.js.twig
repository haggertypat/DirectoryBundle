var listing={
    lat:"{{ listing.location.lat }}",
    lng:"{{ listing.location.lng }}",
    name:"{{ listing.name }}",
    id:"{{ listing.id }}",
    profileLink:"{{ path('profile', {'id' : listing.id }) }}",
    address: "{% if listing.address is defined and listing.address != "" %}{{ listing.address }},{% endif %} {{ listing.city }} {{ listing.state }}"
}; 