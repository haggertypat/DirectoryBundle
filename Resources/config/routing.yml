home:
    pattern:  /
    defaults: { _controller: CCETCDirectoryBundle:Pages:static, template: CCETCDirectoryBundle:Pages:home.html.twig }
    
page:
  pattern: /page/{route}
  defaults: { _controller: CCETCDirectoryBundle:Pages:page }

CCETDirectoryBundle_Listing:
    resource: .
    type: listing